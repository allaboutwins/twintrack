// PWA install prompt is disabled — native App Store / Google Play is the primary install path.
// Keep the file so Layout.tsx import doesn't break; the component renders nothing.
export default function InstallPrompt() {
  return null;
}
